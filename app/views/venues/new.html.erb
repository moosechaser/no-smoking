<% provide(:title, "New Venue") %>

<div class="container">

  <h1>New venue</h1>

  <div class="row">
    <%# if admin not signed in %>
    <%= render 'mailer_form' %>
  </div>
  <%# if admin signed in %>
  <%# <%= render 'form' %>

  <%= link_to 'Back', venues_path %>

</div>